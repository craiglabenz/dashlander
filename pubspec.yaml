name: dashlander
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flame: ^1.37.0
  flutter:
    sdk: flutter
  google_fonts: ^8.1.0
  vector_math: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  shaders:
    - shaders/bloom.frag
    - shaders/mesh_gradient.frag
